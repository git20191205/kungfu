/**
 * D8 - 线程基础与内存可见性
 *
 * <h2>知识点总结</h2>
 *
 * <h3>一、volatile 原理</h3>
 * <pre>
 * ┌─────────────────────────────────────────────────┐
 * │              MESI 缓存一致性协议                  │
 * ├─────────┬─────────┬─────────┬───────────────────┤
 * │ M(修改)  │ E(独占)  │ S(共享)  │ I(无效)           │
 * │ 本地修改  │ 独占干净  │ 多核共享  │ 已失效需重读       │
 * └─────────┴─────────┴─────────┴───────────────────┘
 *
 * volatile 写 → StoreStore屏障 + StoreLoad屏障
 * volatile 读 → LoadLoad屏障  + LoadStore屏障
 *
 * 效果：
 * 1. 可见性 —— 写后立即刷回主内存，读前从主内存加载
 * 2. 有序性 —— 禁止指令重排序
 * 3. 不保证原子性 —— i++ 仍需加锁
 * </pre>
 *
 * <h3>二、synchronized 原理</h3>
 * <pre>
 * 字节码层面：
 *   monitorenter  → 获取对象监视器锁
 *   monitorexit   → 释放对象监视器锁（含异常退出时的释放）
 *
 * 锁升级过程（JDK 6+，不可降级）：
 *
 *   无锁 ──→ 偏向锁 ──→ 轻量级锁 ──→ 重量级锁
 *         CAS设置      CAS自旋       OS互斥量
 *        ThreadID     Mark Word      Monitor
 *
 * ┌──────────┬──────────────────────────────────────┐
 * │ 偏向锁    │ 同一线程多次进入，无需CAS，性能最高     │
 * │ 轻量级锁  │ 不同线程交替进入，CAS自旋，避免阻塞     │
 * │ 重量级锁  │ 多线程竞争激烈，阻塞等待，依赖OS调度     │
 * └──────────┴──────────────────────────────────────┘
 * </pre>
 *
 * <h3>三、JMM 内存模型</h3>
 * <pre>
 * ┌─────────────┐     ┌─────────────┐
 * │   线程 A     │     │   线程 B     │
 * │ ┌─────────┐ │     │ ┌─────────┐ │
 * │ │ 工作内存  │ │     │ │ 工作内存  │ │
 * │ │ (副本)   │ │     │ │ (副本)   │ │
 * │ └────┬────┘ │     │ └────┬────┘ │
 * └──────┼──────┘     └──────┼──────┘
 *    save/load            save/load
 * ┌──────┴───────────────────┴──────┐
 * │           主内存 (堆)            │
 * │      共享变量的权威存储           │
 * └─────────────────────────────────┘
 *
 * 8 种原子操作：
 *   lock → read → load → use → assign → store → write → unlock
 * </pre>
 *
 * <h3>四、happens-before 规则</h3>
 * <pre>
 * 1. 程序顺序规则    —— 同一线程中，前面的操作 HB 后面的操作
 * 2. 监视器锁规则    —— unlock HB 后续 lock
 * 3. volatile 规则   —— volatile 写 HB 后续 volatile 读
 * 4. 线程启动规则    —— start() HB 子线程的每个动作
 * 5. 线程终止规则    —— 子线程的每个动作 HB join() 返回
 * 6. 中断规则        —— interrupt() HB 检测到中断
 * 7. 终结器规则      —— 构造函数 HB finalize()
 * 8. 传递性          —— A HB B，B HB C → A HB C
 * </pre>
 *
 * <h3>五、下一步</h3>
 * <p>Lock 与 AQS —— 从 synchronized 到显式锁，掌握 AQS 核心框架。</p>
 *
 * @author kungfu
 * @since D8
 */
package com.kungfu.concurrent.d1_thread_basis;

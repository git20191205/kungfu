/**
 * D18 - 读写锁
 *
 * <h2>知识点总结</h2>
 *
 * <h3>一、ReentrantReadWriteLock</h3>
 * <pre>
 * 核心规则：
 *   读读共享 — 多个线程可同时持有读锁
 *   读写互斥 — 有线程读时，写线程必须等待
 *   写写互斥 — 同一时刻只能有一个写线程
 *
 * 内部结构：
 *   ┌─────────────────────────────────────────┐
 *   │       ReentrantReadWriteLock             │
 *   │                                         │
 *   │   基于 AQS 的 state（32位 int）          │
 *   │   ┌────────────────┬────────────────┐   │
 *   │   │  高16位：读锁  │  低16位：写锁  │   │
 *   │   │  共享计数      │  独占计数      │   │
 *   │   └────────────────┴────────────────┘   │
 *   │                                         │
 *   │   readLock()  → 共享模式获取             │
 *   │   writeLock() → 独占模式获取             │
 *   └─────────────────────────────────────────┘
 *
 * 适用场景：
 *   - 缓存系统（读多写少）
 *   - 配置管理（频繁读取，偶尔更新）
 *   - 共享数据的并发访问
 * </pre>
 *
 * <h3>二、锁降级</h3>
 * <pre>
 * 锁降级（支持）：写锁 → 获取读锁 → 释放写锁
 *   目的：保证数据修改后，当前线程仍能看到最新值
 *
 *   正确流程：
 *     writeLock.lock();
 *     try {
 *         // 修改数据
 *         readLock.lock();    // 降级：先获取读锁
 *     } finally {
 *         writeLock.unlock(); // 再释放写锁
 *     }
 *     try {
 *         // 使用数据（持有读锁，保证可见性）
 *     } finally {
 *         readLock.unlock();
 *     }
 *
 * 锁升级（不支持）：读锁 → 写锁
 *   原因：多个线程同时持有读锁时，都尝试升级为写锁
 *         → 互相等待对方释放读锁 → 死锁
 * </pre>
 *
 * <h3>三、StampedLock</h3>
 * <pre>
 * JDK 8 引入，提供三种模式：
 *   1. 悲观读锁 — readLock()，类似 ReadWriteLock 的读锁
 *   2. 写锁     — writeLock()，类似 ReadWriteLock 的写锁
 *   3. 乐观读   — tryOptimisticRead()，不加锁，性能最高
 *
 * 乐观读模式：
 *   long stamp = lock.tryOptimisticRead();  // 获取邮戳（不加锁）
 *   // 读取共享数据
 *   if (!lock.validate(stamp)) {            // 验证期间是否有写操作
 *       stamp = lock.readLock();            // 升级为悲观读锁
 *       try {
 *           // 重新读取数据
 *       } finally {
 *           lock.unlockRead(stamp);
 *       }
 *   }
 *
 * 注意事项：
 *   - 不可重入（同一线程不能重复获取）
 *   - 不支持 Condition
 *   - 不能用于 synchronized 块中
 * </pre>
 *
 * <h3>四、对比表</h3>
 * <pre>
 * ┌───────────────────────┬──────┬──────┬──────┬──────┬──────┬────────┐
 * │                       │ 读读 │ 读写 │ 写写 │可重入│公平性│  性能  │
 * ├───────────────────────┼──────┼──────┼──────┼──────┼──────┼────────┤
 * │ synchronized          │ 互斥 │ 互斥 │ 互斥 │  是  │  否  │  一般  │
 * │ ReentrantLock         │ 互斥 │ 互斥 │ 互斥 │  是  │ 可选 │  较好  │
 * │ ReentrantReadWriteLock│ 共享 │ 互斥 │ 互斥 │  是  │ 可选 │  好    │
 * │ StampedLock           │ 共享 │ 互斥 │ 互斥 │  否  │  否  │  最好  │
 * └───────────────────────┴──────┴──────┴──────┴──────┴──────┴────────┘
 * </pre>
 *
 * <h3>五、下一步</h3>
 * <p>W03 Spring 框架 —— 从并发编程进入企业级框架，掌握 IoC、AOP 与 Spring 生态。</p>
 *
 * @author kungfu
 * @since D18
 */
package com.kungfu.concurrent.d10_readwritelock;

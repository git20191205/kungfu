========================================
  ConcurrentHashMap 原理 — 1.7 vs 1.8
========================================

=== 一、HashMap 线程不安全演示 ===

  启动 10 个线程，每个线程 put 1000 个不同 key
  预期 size = 10000

  第 1 次运行: size = 4884 (数据丢失!)
  第 2 次运行: size = 7489 (数据丢失!)

  ⚠ HashMap 并发写入常见问题：
    - 数据丢失（put 覆盖）
    - JDK 7 链表成环导致 CPU 100%（死循环）
    - JDK 8 可能抛出 ConcurrentModificationException

=== 二、ConcurrentHashMap 线程安全验证 ===

  同样 10 个线程，每个线程 put 1000 个不同 key
  预期 size = 10000

  第 1 次运行: size = 10000 (始终正确)
  第 2 次运行: size = 10000 (始终正确)
  第 3 次运行: size = 10000 (始终正确)

=== 三、ConcurrentHashMap 1.7 原理 ===

  核心思想：分段锁（Segment Lock）
  Segment extends ReentrantLock，每个 Segment 守护一段 HashEntry 数组
  不同 Segment 之间可以并发写入，互不干扰
  默认 16 个 Segment → 并发度 = 16

  ┌─────────────────────────────────────────────────────────────┐
  │              ConcurrentHashMap (JDK 1.7)                    │
  ├─────────────────────────────────────────────────────────────┤
  │  Segment[] 数组 (默认长度 16)                               │
  │                                                             │
  │  ┌──────────┐  ┌──────────┐       ┌──────────┐              │
  │  │Segment[0]│  │Segment[1]│  ...  │Segment[15]│             │
  │  │(Lock)    │  │(Lock)    │       │(Lock)     │             │
  │  └────┬─────┘  └────┬─────┘       └────┬──────┘             │
  │       │             │                   │                    │
  │       ▼             ▼                   ▼                    │
  │  ┌─────────┐  ┌─────────┐        ┌─────────┐               │
  │  │HashEntry│  │HashEntry│        │HashEntry│               │
  │  │  数组   │  │  数组   │        │  数组   │               │
  │  └──┬──┬───┘  └──┬──┬───┘        └──┬──┬───┘               │
  │     │  │         │  │                │  │                    │
  │     ▼  ▼         ▼  ▼                ▼  ▼                    │
  │   链表 链表    链表 链表           链表 链表                 │
  └─────────────────────────────────────────────────────────────┘

  put 流程（1.7）：
    1. 根据 key 的 hash 定位到 Segment
    2. 对该 Segment 加 ReentrantLock
    3. 在 Segment 内部的 HashEntry 数组中定位桶
    4. 头插法插入链表
    5. 释放锁

=== 四、ConcurrentHashMap 1.8 原理 ===

  核心变化：废弃 Segment，采用 Node 数组 + 链表/红黑树
  锁粒度从 Segment（多个桶）细化到 单个桶的头节点

  ┌─────────────────────────────────────────────────────────────┐
  │              ConcurrentHashMap (JDK 1.8)                    │
  ├─────────────────────────────────────────────────────────────┤
  │  Node[] table (数组)                                        │
  │                                                             │
  │  [0]       [1]       [2]       [3]       ...   [n-1]        │
  │   │         │         │         │                │           │
  │   ▼         ▼         │         ▼                │           │
  │ ┌───┐    ┌───┐       null    ┌───┐             null         │
  │ │ A │    │ D │               │ G │                          │
  │ └─┬─┘    └─┬─┘               └─┬─┘                          │
  │   ▼        ▼                    ▼                            │
  │ ┌───┐    ┌───┐              ┌──────┐                        │
  │ │ B │    │ E │              │红黑树│                        │
  │ └─┬─┘    └───┘              │(>=8) │                        │
  │   ▼                         └──────┘                        │
  │ ┌───┐                                                       │
  │ │ C │  ← 链表                                              │
  │ └───┘                                                       │
  └─────────────────────────────────────────────────────────────┘

  put 流程（1.8）：
    1. 计算 key 的 hash，定位到数组下标
    2. 如果桶为空 → CAS 写入新 Node（无锁）
    3. 如果正在扩容（hash == MOVED）→ 协助扩容
    4. 否则 → synchronized 锁住桶的头节点，遍历链表/红黑树插入
    5. 链表长度 >= 8 且数组长度 >= 64 → 转红黑树
    6. 链表长度 >= 8 但数组长度 < 64 → 优先扩容

  ┌──────────────┬───────────────────────┬───────────────────────────┐
  │     特性     │       JDK 1.7         │         JDK 1.8           │
  ├──────────────┼───────────────────────┼───────────────────────────┤
  │ 数据结构     │ Segment[] + HashEntry │ Node[] + 链表/红黑树      │
  │              │ + 链表                │                           │
  ├──────────────┼───────────────────────┼───────────────────────────┤
  │ 锁粒度       │ Segment（多个桶）     │ 桶头节点（单个桶）        │
  ├──────────────┼───────────────────────┼───────────────────────────┤
  │ 并发度       │ 默认16（Segment数）   │ 数组长度（更细粒度）      │
  ├──────────────┼───────────────────────┼───────────────────────────┤
  │ 查询复杂度   │ O(n) 链表遍历         │ O(log n) 红黑树           │
  ├──────────────┼───────────────────────┼───────────────────────────┤
  │ hash冲突     │ 链表（头插法）        │ 链表(尾插法)→红黑树       │
  ├──────────────┼───────────────────────┼───────────────────────────┤
  │ 锁实现       │ ReentrantLock         │ CAS + synchronized        │
  └──────────────┴───────────────────────┴───────────────────────────┘

=== 五、size() 的实现演变 ===

  【JDK 1.7 的 size() 实现】
    1. 先不加锁，连续尝试 2 次统计所有 Segment 的 count 之和
    2. 每次统计前后对比 modCount，若一致说明期间无修改，返回结果
    3. 若 2 次尝试都不一致 → 对所有 Segment 加锁后再统计
    4. 这种 "乐观 → 悲观" 的策略避免了不必要的加锁开销

  【JDK 1.8 的 size() 实现】
    采用 baseCount + CounterCell[] 的方式（类似 LongAdder）

    ┌──────────────────────────────────────────────────┐
    │  addCount() 计数流程                             │
    │                                                  │
    │  CAS 更新 baseCount                              │
    │       │                                          │
    │       ├── 成功 → 结束                            │
    │       │                                          │
    │       └── 失败（竞争激烈）                       │
    │              │                                   │
    │              ▼                                   │
    │         CAS 更新 CounterCell[threadHash]         │
    │         (分散竞争到不同 Cell)                    │
    └──────────────────────────────────────────────────┘

    sumCount() = baseCount + sum(CounterCell[])
    最终 size = (int) sumCount()

  实际验证: 放入 100 个元素, size() = 100
  mappingCount() = 100 (推荐使用，返回 long)

========================================
  面试速记
========================================

  1. HashMap 线程不安全的原因：
     - 并发 put 导致数据丢失（覆盖写入）
     - JDK 7 并发扩容导致链表成环 → 死循环
     - JDK 8 改为尾插法解决了成环问题，但仍不安全

  2. CHM 1.7 — Segment 分段锁：
     - Segment extends ReentrantLock
     - 默认 16 个 Segment，并发度 = 16
     - 不同 Segment 之间的操作完全并行

  3. CHM 1.8 — CAS + synchronized：
     - 废弃 Segment，锁粒度细化到桶头节点
     - 空桶用 CAS 无锁写入，非空桶用 synchronized
     - 链表长度 >= 8 且数组 >= 64 → 红黑树

  4. size() 实现差异：
     - 1.7: 先乐观不加锁统计 2 次，不一致则悲观全锁
     - 1.8: baseCount + CounterCell[]（类似 LongAdder 思路）

========================================
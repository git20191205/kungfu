/**
 * D16 - 阻塞队列
 *
 * <h2>知识点总结</h2>
 *
 * <h3>一、BlockingQueue 接口四组 API</h3>
 * <pre>
 * ┌──────────────┬──────────────┬──────────────┬──────────────┐
 * │   方式       │   插入       │   移除       │   检查       │
 * ├──────────────┼──────────────┼──────────────┼──────────────┤
 * │ 抛异常       │ add(e)       │ remove()     │ element()    │
 * ├──────────────┼──────────────┼──────────────┼──────────────┤
 * │ 返回特殊值   │ offer(e)     │ poll()       │ peek()       │
 * ├──────────────┼──────────────┼──────────────┼──────────────┤
 * │ 阻塞         │ put(e)       │ take()       │ —            │
 * ├──────────────┼──────────────┼──────────────┼──────────────┤
 * │ 超时         │ offer(e,t,u) │ poll(t,u)    │ —            │
 * └──────────────┴──────────────┴──────────────┴──────────────┘
 *
 * 四种行为模式：
 *   抛异常    — 队列满时 add 抛 IllegalStateException，空时 remove 抛 NoSuchElementException
 *   返回特殊值 — offer 返回 false，poll 返回 null
 *   阻塞      — put/take 线程挂起，直到空间可用 / 元素可取
 *   超时      — 在指定时间内等待，超时则返回 false/null
 * </pre>
 *
 * <h3>二、ArrayBlockingQueue vs LinkedBlockingQueue</h3>
 * <pre>
 * ┌──────────────────┬─────────────────────────┬─────────────────────────────┐
 * │     特性         │  ArrayBlockingQueue     │  LinkedBlockingQueue        │
 * ├──────────────────┼─────────────────────────┼─────────────────────────────┤
 * │ 底层结构         │ 数组                    │ 链表                        │
 * ├──────────────────┼─────────────────────────┼─────────────────────────────┤
 * │ 是否有界         │ 必须指定容量（有界）     │ 可选（默认 Integer.MAX_VALUE）│
 * ├──────────────────┼─────────────────────────┼─────────────────────────────┤
 * │ 锁的数量         │ 一把锁（ReentrantLock） │ 两把锁（putLock + takeLock） │
 * ├──────────────────┼─────────────────────────┼─────────────────────────────┤
 * │ 吞吐量           │ 较低（put/take 互斥）   │ 较高（put/take 可并行）      │
 * ├──────────────────┼─────────────────────────┼─────────────────────────────┤
 * │ 内存             │ 预分配数组，无 GC 压力  │ 动态分配节点，有 GC 压力     │
 * └──────────────────┴─────────────────────────┴─────────────────────────────┘
 * </pre>
 *
 * <h3>三、SynchronousQueue</h3>
 * <pre>
 * 容量为 0 的特殊队列：
 *   - put() 必须等待另一个线程调用 take()
 *   - 不存储元素，直接在线程之间传递
 *   - Executors.newCachedThreadPool() 使用它
 *   - 适合任务直接交付的场景
 * </pre>
 *
 * <h3>四、DelayQueue</h3>
 * <pre>
 * 延时队列：
 *   - 元素必须实现 Delayed 接口
 *   - 只有延时到期的元素才能被 take()
 *   - 内部使用 PriorityQueue 按到期时间排序
 *   - ScheduledThreadPoolExecutor 的 DelayedWorkQueue 借鉴了类似思想
 *   - 应用场景：定时任务、缓存过期、订单超时
 * </pre>
 *
 * <h3>五、与线程池的关系</h3>
 * <pre>
 * ┌──────────────────────┬──────────────────────┬──────────────────────────────┐
 * │   队列类型           │   对应线程池          │   行为特点                   │
 * ├──────────────────────┼──────────────────────┼──────────────────────────────┤
 * │ LinkedBlockingQueue  │ FixedThreadPool      │ 无界队列，不会创建非核心线程  │
 * ├──────────────────────┼──────────────────────┼──────────────────────────────┤
 * │ SynchronousQueue     │ CachedThreadPool     │ 直接交付，总是创建新线程      │
 * ├──────────────────────┼──────────────────────┼──────────────────────────────┤
 * │ ArrayBlockingQueue   │ 自定义线程池          │ 有界队列，可触发拒绝策略      │
 * ├──────────────────────┼──────────────────────┼──────────────────────────────┤
 * │ DelayedWorkQueue     │ ScheduledThreadPool  │ 按延时排序，定时执行          │
 * └──────────────────────┴──────────────────────┴──────────────────────────────┘
 *
 * 线程池的行为很大程度取决于使用的队列类型：
 *   - 无界队列 → maximumPoolSize 形同虚设，永远不会创建非核心线程
 *   - 有界队列 → 队列满后才创建非核心线程，满了再触发拒绝策略
 *   - 直接交付 → 每个任务都尝试交给线程，没有空闲线程就创建新线程
 * </pre>
 *
 * <h3>六、下一步 → d9_completablefuture CompletableFuture</h3>
 *
 * @author kungfu
 * @since D16
 */
package com.kungfu.concurrent.d8_blockingqueue;

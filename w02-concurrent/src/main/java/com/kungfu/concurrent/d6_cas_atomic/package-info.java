/**
 * D14 - CAS 与原子类
 *
 * <h2>知识点总结</h2>
 *
 * <h3>一、CAS 原理</h3>
 * <pre>
 * CAS = Compare And Swap（比较并交换）
 *
 * 核心思想：
 *   读取内存值 → 比较是否等于预期值 → 相等则更新为新值
 *   整个操作由 CPU 指令（cmpxchg）保证原子性
 *
 * ┌────────────────────────────────────────────────┐
 * │         compareAndSwap(V, Expected, New)       │
 * │                                                │
 * │  内存地址 V 的当前值                            │
 * │       │                                        │
 * │       ▼                                        │
 * │  ┌─────────┐                                   │
 * │  │当前值 == │── 是 ──→ 将 V 更新为 New          │
 * │  │Expected? │         返回 true（成功）         │
 * │  └────┬────┘                                   │
 * │       │ 否                                     │
 * │       ▼                                        │
 * │  不修改，返回 false（失败）                     │
 * │  → 通常配合自旋重试（spin retry）              │
 * └────────────────────────────────────────────────┘
 *
 * Java 实现链路：
 *   AtomicInteger.compareAndSet()
 *     → Unsafe.compareAndSwapInt()
 *       → JNI → CPU cmpxchg 指令（硬件级原子操作）
 * </pre>
 *
 * <h3>二、CAS 三大问题</h3>
 * <pre>
 * ┌──────────────────┬──────────────────────────────────────┐
 * │     问题         │              解决方案                │
 * ├──────────────────┼──────────────────────────────────────┤
 * │ 1. ABA 问题      │ AtomicStampedReference（版本号）     │
 * │                  │ AtomicMarkableReference（布尔标记）  │
 * ├──────────────────┼──────────────────────────────────────┤
 * │ 2. 自旋开销      │ 竞争激烈时 CPU 空转浪费              │
 * │                  │ 可用 LongAdder 分段减少竞争          │
 * ├──────────────────┼──────────────────────────────────────┤
 * │ 3. 只能保证      │ 用 AtomicReference 包装对象          │
 * │    单变量原子性   │ 或用锁保证多变量的原子性             │
 * └──────────────────┴──────────────────────────────────────┘
 *
 * ABA 问题示意：
 *   线程1 读到 A → 被挂起
 *   线程2 将 A → B → A（值变回 A）
 *   线程1 恢复，CAS 比较 A == A → 成功
 *   但实际数据已经被修改过！
 *
 *   解决：加版本号 (A,1) → (B,2) → (A,3)，版本号不同则 CAS 失败
 * </pre>
 *
 * <h3>三、Atomic 原子类族</h3>
 * <pre>
 * ┌──────────────────────────────────────────────────────────┐
 * │                   Atomic 原子类分类                      │
 * ├──────────────────┬─────────────────────────────────────┤
 * │ 基本类型          │ AtomicInteger                       │
 * │                  │ AtomicLong                           │
 * │                  │ AtomicBoolean                        │
 * ├──────────────────┼─────────────────────────────────────┤
 * │ 引用类型          │ AtomicReference                     │
 * │                  │ AtomicStampedReference（解决ABA）    │
 * │                  │ AtomicMarkableReference              │
 * ├──────────────────┼─────────────────────────────────────┤
 * │ 数组类型          │ AtomicIntegerArray                  │
 * │                  │ AtomicLongArray                      │
 * │                  │ AtomicReferenceArray                 │
 * ├──────────────────┼─────────────────────────────────────┤
 * │ 字段更新器        │ AtomicIntegerFieldUpdater           │
 * │                  │ AtomicLongFieldUpdater               │
 * │                  │ AtomicReferenceFieldUpdater          │
 * ├──────────────────┼─────────────────────────────────────┤
 * │ 累加器（JDK 8）   │ LongAdder / LongAccumulator        │
 * │                  │ DoubleAdder / DoubleAccumulator     │
 * └──────────────────┴─────────────────────────────────────┘
 * </pre>
 *
 * <h3>四、LongAdder vs AtomicLong</h3>
 * <pre>
 * AtomicLong：
 *   所有线程竞争同一个 value 变量
 *   高并发下 CAS 频繁失败 → 大量自旋 → 性能下降
 *
 * LongAdder（JDK 8 引入）：
 *   分段思想 → 将竞争分散到多个 Cell
 *
 *   ┌──────────┐
 *   │  base    │ ←── 无竞争时 CAS 更新
 *   └──────────┘
 *   ┌────────┬────────┬────────┬────────┐
 *   │Cell[0] │Cell[1] │Cell[2] │Cell[3] │ ←── 竞争时分散到不同 Cell
 *   └────────┴────────┴────────┴────────┘
 *
 *   sum() = base + Cell[0] + Cell[1] + ... + Cell[n]
 *
 * 对比：
 * ┌──────────────┬──────────────────┬──────────────────┐
 * │              │   AtomicLong     │   LongAdder      │
 * ├──────────────┼──────────────────┼──────────────────┤
 * │ 低并发       │ 性能相当          │ 性能相当          │
 * │ 高并发       │ CAS 竞争激烈      │ 分段减少竞争      │
 * │ 精确读取     │ get() 精确        │ sum() 非精确     │
 * │ 适用场景     │ 需精确值          │ 统计计数          │
 * └──────────────┴──────────────────┴──────────────────┘
 * </pre>
 *
 * <h3>五、下一步</h3>
 * <p>d7_threadlocal ThreadLocal —— 线程本地变量，从共享到隔离的并发思路。</p>
 *
 * @author kungfu
 * @since D14
 */
package com.kungfu.concurrent.d6_cas_atomic;

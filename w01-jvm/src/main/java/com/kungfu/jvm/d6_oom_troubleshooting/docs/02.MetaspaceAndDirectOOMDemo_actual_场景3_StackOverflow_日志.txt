========================================
  非堆 OOM 排查实战
========================================

=== OOM 类型速查表 ===

  ┌──────────────────────────────────────┬───────────────────────┬──────────────────────────┐
  │ 错误信息                             │ 内存区域              │ 常见根因                  │
  ├──────────────────────────────────────┼───────────────────────┼──────────────────────────┤
  │ java.lang.OutOfMemoryError:          │                       │                          │
  │   Java heap space                    │ 堆                    │ 对象泄漏/堆太小          │
  │   GC overhead limit exceeded         │ 堆                    │ GC 回收率 < 2%           │
  │   Metaspace                          │ 元空间（本地内存）    │ 类太多/ClassLoader泄漏   │
  │   Direct buffer memory               │ 直接内存（本地内存）  │ NIO ByteBuffer 泄漏      │
  │   unable to create new native thread │ 操作系统              │ 线程数超过 OS 限制        │
  ├──────────────────────────────────────┼───────────────────────┼──────────────────────────┤
  │ java.lang.StackOverflowError         │ 线程栈                │ 递归过深/栈帧过大        │
  └──────────────────────────────────────┴───────────────────────┴──────────────────────────┘

--- 场景三：StackOverflowError ---

  模拟：无限递归调用
  典型原因：递归缺少终止条件 / 循环依赖调用

  递归深度: 1000
  递归深度: 2000

  ★ StackOverflow！递归深度达到 2719 层
  错误: StackOverflowError

  排查流程：
  1. 看异常堆栈 → 相同方法反复出现 → 递归
  2. 检查递归终止条件是否正确
  3. 计算递归深度是否合理（默认栈大小约 512KB~1MB）

  每个栈帧的大小取决于：
    - 局部变量数量和类型
    - 操作数栈深度
    - 大约 128~256 字节/帧

  修复方案：
  1. 修复递归终止条件
  2. 递归改迭代（用显式栈代替调用栈）
  3. 调大栈空间: -Xss512k（不推荐，只是治标）
  4. 尾递归优化（Java 不原生支持，需手动改写）

  ★ 面试加分项：
    -Xss 影响每个线程的栈大小，不是总栈大小
    -Xss 越大 → 每个线程占用内存越多 → 可创建线程数越少
    默认 -Xss: Linux 64位 = 1MB, Windows = 320KB~1MB

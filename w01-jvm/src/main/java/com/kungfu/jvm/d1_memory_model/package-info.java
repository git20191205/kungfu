/**
 * D1 - JVM 内存模型
 *
 * <h2>知识点总结</h2>
 *
 * <h3>一、JVM 运行时数据区（五大区域）</h3>
 * <pre>
 * ┌─────────────────────────────────────────────────┐
 * │                  JVM 运行时数据区                  │
 * ├─────────────────────┬───────────────────────────┤
 * │    线程私有           │       线程共享              │
 * │                     │                           │
 * │  ┌───────────────┐  │  ┌─────────────────────┐  │
 * │  │ 虚拟机栈       │  │  │ 堆 (Heap)            │  │
 * │  │ (VM Stack)    │  │  │ - 对象实例            │  │
 * │  │ - 栈帧        │  │  │ - 数组               │  │
 * │  │ - 局部变量表   │  │  │ - GC 的主战场         │  │
 * │  └───────────────┘  │  └─────────────────────┘  │
 * │  ┌───────────────┐  │  ┌─────────────────────┐  │
 * │  │ 本地方法栈     │  │  │ 方法区 (Metaspace)    │  │
 * │  │ (Native Stack)│  │  │ - 类信息              │  │
 * │  │ - native方法   │  │  │ - 常量池              │  │
 * │  └───────────────┘  │  │ - 静态变量            │  │
 * │  ┌───────────────┐  │  └─────────────────────┘  │
 * │  │ 程序计数器     │  │                           │
 * │  │ (PC Register) │  │                           │
 * │  │ - 字节码行号   │  │                           │
 * │  └───────────────┘  │                           │
 * └─────────────────────┴───────────────────────────┘
 * </pre>
 *
 * <h3>二、对象分配策略</h3>
 * <ol>
 *   <li>对象优先在 Eden 区分配</li>
 *   <li>大对象直接进入老年代（-XX:PretenureSizeThreshold）</li>
 *   <li>长期存活对象晋升老年代（-XX:MaxTenuringThreshold，默认15）</li>
 *   <li>动态年龄判定：Survivor 区同龄对象超过一半空间 → 提前晋升</li>
 * </ol>
 *
 * <h3>三、关键 JVM 参数速查</h3>
 * <pre>
 * -Xms / -Xmx          堆的初始/最大大小（生产环境建议设为一样）
 * -Xmn                  新生代大小
 * -XX:SurvivorRatio     Eden:Survivor 比例（默认 8:1:1）
 * -XX:+PrintGCDetails   打印 GC 详情（JDK 8）
 * -Xlog:gc*             打印 GC 详情（JDK 9+）
 * -XX:PretenureSizeThreshold  大对象阈值（仅 Serial/ParNew）
 * -XX:MaxTenuringThreshold    晋升年龄阈值（默认 15）
 * </pre>
 *
 * <h3>四、面试高频问题</h3>
 * <ul>
 *   <li>Q: JVM 内存区域有哪些？哪些是线程私有的？</li>
 *   <li>Q: 堆和栈的区别是什么？</li>
 *   <li>Q: 对象一定分配在堆上吗？（逃逸分析 → 栈上分配）</li>
 *   <li>Q: 方法区在 JDK 7 和 JDK 8 有什么变化？（永久代 → 元空间）</li>
 * </ul>
 *
 * <h3>五、下一步</h3>
 * D2 将学习 GC 算法（标记-清除、标记-复制、标记-整理）和主流垃圾回收器（CMS、G1、ZGC）。
 *
 * @author kungfu
 * @since D1
 */
package com.kungfu.jvm.d1_memory_model;
